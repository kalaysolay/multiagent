# Тестовый профиль: pgvector (всегда), embedding-provider=OLLAMA для совместимости с mock
app:
  embedding-provider: OLLAMA

spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

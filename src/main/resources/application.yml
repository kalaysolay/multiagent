server:
  port: 8080

spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY: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}
      chat:
        options:
          model: gpt-3.5-turbo
          temperature: 1.0
  
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:iconix_agent_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:123qweasd}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

app:
  # Провайдер для векторного хранилища: OPENAI или DEEPSEEK
  vector-store-provider: ${VECTOR_STORE_PROVIDER:OPENAI}
  # Провайдер для LLM: OPENAI или DEEPSEEK
  llm-provider: ${LLM_PROVIDER:OPENAI}
  openai:
    vector-store-id: ${OPENAI_VECTOR_STORE_ID:vs_68e649a20d0c8191aaa619e4edeeb03a}
  # Конфигурация для DeepSeek LLM (использует OpenAI-совместимый API)
  deepseek:
    api-key: ${DEEPSEEK_API_KEY:}
    base-url: ${DEEPSEEK_BASE_URL:https://api.deepseek.com}
    model: ${DEEPSEEK_MODEL:deepseek-chat}
  # Конфигурация для локального векторного хранилища (pgvector)
  vector-store:
    table-name: document_embeddings
    embedding-dimensions: 1536  # Размерность для OpenAI text-embedding-ada-002
